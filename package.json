{
  "name": "treasure-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "standard": "standard --fix",
    "build": "browserify src/index.js src/htmlserv.js -o dist/bundle.js && echo 'built!'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "clone": "^2.1.0",
    "express": "^4.14.0",
    "pixi.js": "^4.2.3"
  },
  "devDependencies": {
    "browserify": "^13.1.1",
    "standard": "^8.6.0"
  }
}
